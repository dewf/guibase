module Foundation;

opaque CFString {
    static CFString makeConstant(string s);
    static CFString create(string s);
}

enum URLPathStyle {
    POSIX = 0,
    // type 1 deprecated
    Windows = 2
}

opaque URL {
    static URL createWithFileSystemPath(CFString path, URLPathStyle pathStyle, bool isDirectory);
}
